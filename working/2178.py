# 미로 탐색 - 최소 칸

# bfs함수
def bfs(graph,visit):
    
    count = []
    
    for i in range():
    
    return graph

# N는 행 M은 열
N,M = map(int,input().split(' '))

graph = []
visit = [[False]*M]*N

for i in range(N):
    a = list(map(int,input()))
    graph.append(a)
    
bfs(graph,visit)
    
